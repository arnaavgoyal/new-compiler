def (*i8) i32
foo(str) {
    return 0;
}

type myty = i32;

var i32 glob = 0;

def (i32, **i8) i32
main(argc, argv) {
    type charstar = *i8;
    type i32ptr = *i32;
    var charstar str = *argv;
    var i32ptr ptr = argv;
    //var []*()i32 str;
    //str++;
    var i32 val = 0;
    var u16 val2 = -001;
    var u8 thing;
    *str = 5;
    //str = *argv;
    while (val) {
        val = foo(str);
        if (val) {
            val = 1;
        }
        else {
            val = 400;
        }
    }
    if (0 as i32 == argc) {
        str = *argv;
        val = foo(*argv);
    }
    else if (argc as u8 == 2) {
        str = *argv;
    }
    foo(str);
    return val;
}

// ------------------------------