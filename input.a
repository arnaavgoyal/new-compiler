def (*i8) i32
foo(str) {
    return 0;
}

type myty = i32;
//type myty = hehehe;

def (i32, **i8) i32
main(argc, argv) {
    var *i8 str = *argv;
    //var []*()i32 str;
    //str++;
    var i32 val = 0;
    //str = *argv;
    while (val) {
        val = foo(str);
        if (val) {
            val = 1;
        }
        else {
            val = 4;
        }
    }
    if (argc == 0) {
        str = *argv;
        val = foo(*argv);
    }
    else if (argc == 2) {
        str = *argv;
    }
    foo(str);
    return val;
}

// ------------------------------