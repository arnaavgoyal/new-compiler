type my_i32 = i32;

def (i32) i32
foo(num) {
    return num;
}

def (*i8) i32
bar(str) {
    var i32 i = 14;
    var i32 v = foo(i);
    return v;
}

def (i32, **i8) i32
my_main(argc, argv) {
    var i32 v = 3;
    v = bar(*argv);
    return v;
}
